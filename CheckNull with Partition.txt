import scala.io.Source
import java.io.File

object SomeApp extends App {

  val readFile = Source.fromFile(new File("/CTRLFW/SCUDEE/bala/TL_THRESHOLD_ROLLUP.D2017051.T221737601.R000006")).getLines()
  val indices = List(1,0)
  val (invalid, valid) = readFile.partition { line =>
    val pkeyCandidates = indices.collect(line.split('\u0001'))
    //println ("PKey candidates "+pkeyCandidates)
   if(pkeyCandidates.exists(_==""))

   //Check if at least one of the columns in the indices has value
  }